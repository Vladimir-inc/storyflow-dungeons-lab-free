{{!-- Imperatively-managed host for the LiteGraph <canvas>. This PART is rendered once
      and never re-rendered - the adapter owns its contents. tabindex makes it focusable
      so canvas-scoped keyboard shortcuts (copy/paste/duplicate/delete) fire here only. --}}
<div class="storyflow-graph-host" tabindex="-1"></div>
